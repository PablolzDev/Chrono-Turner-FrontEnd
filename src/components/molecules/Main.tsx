import React from 'react';
import { Content } from '@styles/styles';
import FeatureSection from './FeatureSection';

const Main: React.FC = () => (
  <Content>
    <section>
      <FeatureSection />
    </section>
  </Content>
);

export default Main;